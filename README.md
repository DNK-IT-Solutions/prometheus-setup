After pulling to the server please make some changes:

In the prometheus.yml change INSTANCE_NAME variable in global directive

In the alertmanager.yml change SLACK_API_URL variable in global directive 